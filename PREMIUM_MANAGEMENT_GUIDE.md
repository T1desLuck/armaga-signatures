# 🎁 РУКОВОДСТВО ПО УПРАВЛЕНИЮ PREMIUM ПОДПИСКАМИ

## 📋 ОБЗОР СИСТЕМЫ

Теперь ты можешь **бесплатно давать Premium** своим знакомым двумя способами:

1. **Промокоды** - пользователь вводит код в приложении
2. **Удаленная активация** - ты добавляешь email в Firebase

---

## 🔧 НАСТРОЙКА FIREBASE FIRESTORE

### Шаг 1: Включи Firestore

1. Открой [Firebase Console](https://console.firebase.google.com/)
2. Выбери проект `armaga-5a9ee`
3. Перейди в **Firestore Database**
4. Нажми **Create database**
5. Выбери **Production mode**
6. Выбери регион (например, `europe-west1`)

### Шаг 2: Настрой правила безопасности

В разделе **Rules** вставь:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Промокоды - только чтение
    match /promo_codes/{code} {
      allow read: if request.auth != null;
      allow write: if false; // Только через консоль
    }
    
    // Premium пользователи - только чтение своих данных
    match /premium_users/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if false; // Только через консоль или промокод
    }
  }
}
```

---

## 🎟️ СПОСОБ 1: ПРОМОКОДЫ

### Создание промокода:

1. Открой **Firestore Database** в Firebase Console
2. Создай коллекцию `promo_codes`
3. Добавь документ с ID = код промокода (например, `FRIEND2024`)
4. Добавь поля:

```json
{
  "code": "FRIEND2024",
  "durationDays": 365,
  "maxUses": 1,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

**Поля:**
- `code` - сам промокод (заглавными буквами)
- `durationDays` - количество дней Premium (365 = 1 год)
- `maxUses` - сколько раз можно использовать (1 = одноразовый)
- `usedCount` - счетчик использований (начинается с 0)
- `isActive` - активен ли промокод (true/false)
- `createdBy` - кто создал (для статистики)
- `usedBy` - массив userId кто использовал

### Примеры промокодов:

**Одноразовый на 1 год:**
```json
{
  "code": "BESTFRIEND",
  "durationDays": 365,
  "maxUses": 1,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

**Многоразовый на 30 дней (для 10 человек):**
```json
{
  "code": "PROMO30",
  "durationDays": 30,
  "maxUses": 10,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

**Пожизненный (100 лет):**
```json
{
  "code": "LIFETIME",
  "durationDays": 36500,
  "maxUses": 1,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

### Как пользователь активирует:

1. Открывает приложение
2. Авторизуется через Google
3. Идет в **Настройки**
4. Нажимает **"Ввести промокод"**
5. Вводит код (например, `FRIEND2024`)
6. Нажимает **"Активировать"**
7. ✅ Premium активирован!

---

## 👤 СПОСОБ 2: УДАЛЕННАЯ АКТИВАЦИЯ ПО EMAIL

### Активация вручную:

1. Узнай **email** знакомого (тот, которым он авторизовался в приложении)
2. Узнай его **userId** (можно посмотреть в Firebase Authentication)
3. Открой **Firestore Database**
4. Создай коллекцию `premium_users` (если еще нет)
5. Создай документ с ID = `userId` пользователя
6. Добавь поля:

```json
{
  "email": "friend@example.com",
  "isPremium": true,
  "expiresAt": 1735689600000,
  "grantedBy": "admin",
  "grantedAt": 1704153600000
}
```

**Поля:**
- `email` - email пользователя
- `isPremium` - всегда `true`
- `expiresAt` - timestamp когда истекает (в миллисекундах)
- `grantedBy` - кто выдал (например, "admin")
- `grantedAt` - timestamp когда выдано

### Как вычислить timestamp:

**Онлайн калькулятор:** https://www.epochconverter.com/

**Или используй JavaScript в консоли браузера:**

```javascript
// На 1 год вперед
new Date().getTime() + (365 * 24 * 60 * 60 * 1000)

// На 30 дней вперед
new Date().getTime() + (30 * 24 * 60 * 60 * 1000)

// Пожизненно (100 лет)
new Date().getTime() + (36500 * 24 * 60 * 60 * 1000)
```

### Пример:

Сегодня 24 ноября 2025, хочешь дать Premium на 1 год:

```javascript
// Текущее время: 1732435200000
// +1 год: 1763971200000

{
  "email": "friend@gmail.com",
  "isPremium": true,
  "expiresAt": 1763971200000,
  "grantedBy": "admin",
  "grantedAt": 1732435200000
}
```

---

## 📊 МОНИТОРИНГ И УПРАВЛЕНИЕ

### Просмотр активных Premium пользователей:

1. Открой **Firestore Database**
2. Коллекция `premium_users`
3. Видишь всех, кому выдан Premium

### Отключение Premium:

**Вариант 1:** Удали документ пользователя из `premium_users`

**Вариант 2:** Измени `expiresAt` на прошедшую дату:
```json
{
  "expiresAt": 1000000000000
}
```

### Деактивация промокода:

Измени поле `isActive` на `false`:
```json
{
  "isActive": false
}
```

### Статистика использования промокода:

Смотри поля:
- `usedCount` - сколько раз использован
- `usedBy` - массив userId кто использовал

---

## 🎯 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Дать другу Premium на год

**Через промокод:**
1. Создай промокод `FRIEND2024` на 365 дней, 1 использование
2. Отправь другу код
3. Друг вводит в приложении
4. ✅ Готово!

**Через Firebase:**
1. Узнай email друга
2. Найди его userId в Firebase Authentication
3. Создай документ в `premium_users` с его userId
4. Установи `expiresAt` на год вперед
5. ✅ Готово! При следующем запуске приложения у него будет Premium

### Пример 2: Раздать 10 промокодов на 30 дней

Создай промокод:
```json
{
  "code": "PROMO30",
  "durationDays": 30,
  "maxUses": 10,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

Раздай код `PROMO30` 10 друзьям. Каждый сможет активировать.

### Пример 3: Пожизненный Premium для себя

Создай промокод:
```json
{
  "code": "MYLIFETIME",
  "durationDays": 36500,
  "maxUses": 1,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

Активируй в приложении → 100 лет Premium 🎉

---

## 🔒 БЕЗОПАСНОСТЬ

### Правила:

1. **Не публикуй промокоды публично** - только друзьям
2. **Ограничивай maxUses** - чтобы не раздали всем подряд
3. **Проверяй usedBy** - кто использовал промокод
4. **Деактивируй старые коды** - установи `isActive: false`

### Защита от злоупотреблений:

- Промокод можно использовать только **один раз на пользователя**
- Проверка `usedBy` массива предотвращает повторное использование
- Только авторизованные пользователи могут активировать

---

## 📱 КАК ЭТО РАБОТАЕТ В ПРИЛОЖЕНИИ

### Проверка Premium статуса:

1. Приложение проверяет **Firebase Firestore** (`premium_users`)
2. Если найдена запись с `isPremium: true` и `expiresAt > сейчас` → Premium активен
3. Если нет записи в Firestore → проверяет Google Play подписку
4. Если есть подписка → Premium активен

### Приоритет:

**Firebase Premium > Google Play подписка**

Это значит, что даже если у пользователя нет подписки, но ты дал ему Premium через Firebase - он будет Premium!

---

## 🎁 ГОТОВЫЕ ШАБЛОНЫ ПРОМОКОДОВ

### Для друзей (1 год):
```json
{
  "code": "FRIEND2025",
  "durationDays": 365,
  "maxUses": 1,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

### Для тестирования (7 дней):
```json
{
  "code": "TEST7",
  "durationDays": 7,
  "maxUses": 100,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

### VIP (пожизненно):
```json
{
  "code": "VIP2025",
  "durationDays": 36500,
  "maxUses": 1,
  "usedCount": 0,
  "isActive": true,
  "createdBy": "admin",
  "usedBy": []
}
```

---

## ✅ ЧЕКЛИСТ НАСТРОЙКИ

- [ ] Включил Firestore в Firebase Console
- [ ] Настроил правила безопасности
- [ ] Создал коллекцию `promo_codes`
- [ ] Создал первый тестовый промокод
- [ ] Протестировал активацию в приложении
- [ ] Создал коллекцию `premium_users` (опционально)
- [ ] Добавил себя в Premium (опционально)

---

## 🚀 ГОТОВО!

Теперь ты можешь бесплатно давать Premium своим друзьям!

**Быстрый старт:**
1. Открой Firebase Console
2. Firestore Database → Создай коллекцию `promo_codes`
3. Добавь документ `FRIEND2024` с полями из примера
4. Отправь код другу
5. Друг вводит в приложении → Premium активирован! 🎉

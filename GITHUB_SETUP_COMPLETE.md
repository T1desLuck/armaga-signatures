# 📦 ПОЛНАЯ ИНСТРУКЦИЯ ПО НАСТРОЙКЕ GITHUB РЕПОЗИТОРИЯ ДЛЯ ARMAGA

## 🎯 ЦЕЛЬ
Настроить GitHub репозиторий для автоматического обновления антивирусных сигнатур в приложении Armaga.

---

## 📋 ШАГ 1: СОЗДАНИЕ СТРУКТУРЫ РЕПОЗИТОРИЯ

### 1.1 Создайте следующую структуру папок в вашем GitHub репозитории:

```
armaga-signatures/
├── signatures/
│   └── signatures.zip
├── metadata.json
└── README.md
```

---

## 📝 ШАГ 2: СОЗДАНИЕ ФАЙЛА metadata.json

Создайте файл `metadata.json` в корне репозитория со следующим содержимым:

```json
{
  "version": "1.0.0",
  "timestamp": 1700000000000,
  "signature_count": 1000,
  "download_url": "https://raw.githubusercontent.com/ВАШ_USERNAME/armaga-signatures/main/signatures/signatures.zip",
  "checksum": "MD5_HASH_ВАШЕГО_ZIP_ФАЙЛА",
  "release_notes": "Initial release with 1000 malware signatures"
}
```

**Замените:**
- `ВАШ_USERNAME` - на ваш GitHub username
- `MD5_HASH_ВАШЕГО_ZIP_ФАЙЛА` - на реальный MD5 хеш (см. Шаг 4)

---

## 📦 ШАГ 3: СОЗДАНИЕ signatures.zip

### 3.1 Создайте текстовый файл `signatures.txt` со следующим форматом:

```
# MD5 хеши вредоносных файлов
5d41402abc4b2a76b9719d911017c592:Trojan.Generic
098f6bcd4621d373cade4e832627b4f6:Malware.Test
ad0234829205b9033196ba818f7a872b:Virus.Win32

# SHA256 хеши
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855:Ransomware.Cryptor
```

**Формат:** `HASH:THREAT_NAME`

### 3.2 Упакуйте файл в ZIP:

**Windows:**
```cmd
powershell Compress-Archive -Path signatures.txt -DestinationPath signatures.zip
```

**Linux/Mac:**
```bash
zip signatures.zip signatures.txt
```

### 3.3 Поместите `signatures.zip` в папку `signatures/`

---

## 🔐 ШАГ 4: ПОЛУЧЕНИЕ MD5 ХЕША

### Windows PowerShell:
```powershell
Get-FileHash signatures.zip -Algorithm MD5
```

### Linux/Mac:
```bash
md5sum signatures.zip
```

Скопируйте полученный хеш и вставьте в `metadata.json` в поле `checksum`.

---

## 🚀 ШАГ 5: ЗАГРУЗКА В GITHUB

### 5.1 Через Git командную строку:

```bash
# Инициализация репозитория (если еще не сделано)
git init
git remote add origin https://github.com/ВАШ_USERNAME/armaga-signatures.git

# Добавление файлов
git add .
git commit -m "Initial commit: Add antivirus signatures"
git branch -M main
git push -u origin main
```

### 5.2 Через GitHub Web Interface:

1. Откройте https://github.com/ВАШ_USERNAME/armaga-signatures
2. Нажмите "Add file" → "Upload files"
3. Перетащите все файлы
4. Нажмите "Commit changes"

---

## 🔗 ШАГ 6: НАСТРОЙКА ПРИЛОЖЕНИЯ

### 6.1 Откройте файл `Constants.kt` в проекте:

```kotlin
// app/src/main/java/com/armaga/antivirus/utils/Constants.kt

object Constants {
    // ...
    
    // GitHub Configuration
    const val GITHUB_METADATA_URL = "https://raw.githubusercontent.com/ВАШ_USERNAME/armaga-signatures/main/metadata.json"
    const val GITHUB_TOKEN = "ВАШ_GITHUB_TOKEN" // Опционально для приватных репозиториев
}
```

**Замените:**
- `ВАШ_USERNAME` - на ваш GitHub username
- `ВАШ_GITHUB_TOKEN` - на ваш Personal Access Token (если репозиторий приватный)

---

## 🔑 ШАГ 7: СОЗДАНИЕ GITHUB TOKEN (Опционально)

Если ваш репозиторий приватный:

1. Откройте https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Дайте имя: `Armaga Signatures Access`
4. Выберите срок действия: `No expiration` или нужный период
5. Отметьте права: `repo` (Full control of private repositories)
6. Нажмите "Generate token"
7. **СКОПИРУЙТЕ ТОКЕН СРАЗУ!** Он больше не будет показан
8. Вставьте токен в `Constants.kt` в поле `GITHUB_TOKEN`

---

## 🔄 ШАГ 8: ОБНОВЛЕНИЕ СИГНАТУР

### Когда нужно обновить сигнатуры:

1. **Обновите файл `signatures.txt`** - добавьте новые хеши
2. **Пересоздайте `signatures.zip`**
3. **Получите новый MD5 хеш**
4. **Обновите `metadata.json`:**
   ```json
   {
     "version": "1.1.0",  // Увеличьте версию
     "timestamp": 1700100000000,  // Новая временная метка
     "signature_count": 1500,  // Новое количество
     "download_url": "...",
     "checksum": "НОВЫЙ_MD5_HASH",
     "release_notes": "Added 500 new signatures"
   }
   ```
5. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Update signatures v1.1.0"
   git push
   ```

---

## ✅ ШАГ 9: ПРОВЕРКА РАБОТЫ

### 9.1 В приложении:

1. Откройте Armaga
2. Перейдите в Settings
3. Нажмите "Check for Updates"
4. Приложение должно:
   - Скачать `metadata.json`
   - Сравнить версии
   - Скачать `signatures.zip`
   - Проверить MD5
   - Распаковать и загрузить в базу данных

### 9.2 Проверка вручную:

Откройте в браузере:
```
https://raw.githubusercontent.com/ВАШ_USERNAME/armaga-signatures/main/metadata.json
```

Вы должны увидеть JSON с метаданными.

---

## 🛠️ TROUBLESHOOTING

### Проблема: "Failed to download metadata"

**Решение:**
- Проверьте URL в `Constants.kt`
- Убедитесь, что репозиторий публичный или токен правильный
- Проверьте интернет-соединение

### Проблема: "Checksum mismatch"

**Решение:**
- Пересчитайте MD5 хеш файла `signatures.zip`
- Обновите значение в `metadata.json`
- Закоммитьте изменения

### Проблема: "Invalid ZIP format"

**Решение:**
- Убедитесь, что ZIP создан правильно
- Проверьте, что внутри ZIP есть `signatures.txt`
- Пересоздайте ZIP файл

---

## 📊 РЕКОМЕНДАЦИИ

### Частота обновлений:
- **Критические угрозы:** Немедленно
- **Обычные обновления:** 1-2 раза в неделю
- **Плановые:** Раз в месяц

### Размер файла:
- Оптимально: 100-500 KB
- Максимум: 5 MB
- При превышении - разбейте на части

### Версионирование:
- Используйте семантическое версионирование: `MAJOR.MINOR.PATCH`
- `MAJOR` - критические изменения
- `MINOR` - добавление новых сигнатур
- `PATCH` - исправления

---

## 🎉 ГОТОВО!

Ваш GitHub репозиторий настроен и готов к использованию!

Приложение Armaga теперь будет автоматически проверять и загружать обновления сигнатур.

---

## 📞 ПОДДЕРЖКА

Если возникли проблемы:
1. Проверьте все URL и токены
2. Убедитесь, что файлы загружены на GitHub
3. Проверьте логи приложения
4. Пересоздайте файлы с нуля, следуя инструкции

**Успехов! 🚀**

# 📚 ПОЛНОЕ РУКОВОДСТВО ПО БАЗЕ ДАННЫХ ВИРУСОВ ARMAGA ANTIVIRUS

## 📋 СОДЕРЖАНИЕ
1. [Обзор системы](#обзор-системы)
2. [Как это работает](#как-это-работает)
3. [Настройка GitHub репозитория](#настройка-github-репозитория)
4. [Создание базы данных вирусов](#создание-базы-данных-вирусов)
5. [Источники данных о вирусах](#источники-данных-о-вирусах)
6. [Обновление базы данных](#обновление-базы-данных)
7. [Автообновление для Premium](#автообновление-для-premium)
8. [Проверка работы](#проверка-работы)
9. [Troubleshooting](#troubleshooting)

---

## 🎯 ОБЗОР СИСТЕМЫ

### Что реализовано:

✅ **GitHub репозиторий** как бесплатное хранилище баз данных  
✅ **Автоматическая загрузка** базы при первом запуске приложения  
✅ **Ручное обновление** через кнопку на главном экране  
✅ **Автообновление** каждые 12 часов (только для Premium подписчиков)  
✅ **Проверка целостности** через SHA256  
✅ **Использование в сканировании** - база проверяется при каждом сканировании  
✅ **Работа без базы** - если база отсутствует, используется эвристический анализ  

### Технические детали:

- **Репозиторий:** `https://github.com/T1desLuck/armaga-signatures`
- **Токен доступа:** Уже настроен в `AppConfig.kt`
- **Формат базы:** ZIP архив с текстовыми файлами MD5 и SHA256 хешей
- **Размер базы:** Рекомендуется 100-500 KB, максимум 5 MB
- **Частота обновлений:** Автоматически каждые 12 часов для Premium

---

## 🔄 КАК ЭТО РАБОТАЕТ

### Процесс сканирования:

```
1. Файл сканируется → Вычисляется MD5 и SHA256 хеш
2. Хеш проверяется в локальной базе данных (Room/SQLite)
3. Если найдено совпадение → УГРОЗА ОБНАРУЖЕНА
4. Если не найдено → Проверка через эвристику + Google Play Protect
```

### Процесс обновления:

```
1. Приложение загружает metadata.json с GitHub
2. Сравнивает версию с текущей
3. Если новая версия → Скачивает ZIP файл
4. Проверяет SHA256 целостность
5. Распаковывает и загружает в базу данных
6. Показывает уведомление об успешном обновлении
```

### Автообновление (Premium):

```
1. WorkManager запускается каждые 12 часов
2. Проверяет: включено ли автообновление + есть ли Premium
3. Проверяет наличие обновлений
4. Скачивает и устанавливает автоматически
5. Уведомляет пользователя
```

---

## 🛠️ НАСТРОЙКА GITHUB РЕПОЗИТОРИЯ

### Шаг 1: Структура репозитория

Ваш репозиторий уже создан: `https://github.com/T1desLuck/armaga-signatures`

Создайте следующую структуру:

```
armaga-signatures/
├── databases/
│   ├── metadata.json
│   └── signatures-v2024.11.24.zip
└── README.md
```

### Шаг 2: Создайте metadata.json

Создайте файл `databases/metadata.json`:

```json
{
  "version": "2024.11.24",
  "download_url": "https://raw.githubusercontent.com/T1desLuck/armaga-signatures/main/databases/signatures-v2024.11.24.zip",
  "sha256": "ВСТАВЬТЕ_СЮДА_SHA256_ХЕША",
  "size": 102400,
  "updated_at": "2024-11-24T12:00:00Z",
  "signatures_count": 5000
}
```

**Важно:**
- `version` - дата в формате YYYY.MM.DD или YYYY.MM.DD-HH
- `download_url` - прямая ссылка на raw.githubusercontent.com
- `sha256` - SHA256 хеш ZIP файла (вычислим позже)
- `size` - размер ZIP файла в байтах
- `signatures_count` - количество сигнатур в базе

---

## 📦 СОЗДАНИЕ БАЗЫ ДАННЫХ ВИРУСОВ

### Шаг 1: Создайте файлы сигнатур

#### md5_hashes.txt

Формат: `MD5_HASH,THREAT_NAME,SEVERITY`

```
d41d8cd98f00b204e9800998ecf8427e,Android.Trojan.FakeBank,CRITICAL
098f6bcd4621d373cade4e832627b4f6,Android.Adware.Generic,MEDIUM
5d41402abc4b2a76b9719d911017c592,Android.Spyware.SMS,HIGH
7c4a8d09ca3762af61e59520943dc26494f8941b,Android.Malware.Test,LOW
```

#### sha256_hashes.txt

Формат: `SHA256_HASH,THREAT_NAME,SEVERITY`

```
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855,Android.Trojan.Banker,CRITICAL
6b86b273ff34fce19d6b804eff5a3f5747ada4eaa22f1d49c01e52ddb7875b4b,Android.Ransomware.Cryptor,CRITICAL
```

**Уровни опасности (SEVERITY):**
- `CRITICAL` - Критическая угроза
- `HIGH` - Высокая опасность
- `MEDIUM` - Средняя опасность
- `LOW` - Низкая опасность

### Шаг 2: Создайте ZIP архив

**Windows PowerShell:**
```powershell
mkdir signatures
copy md5_hashes.txt signatures\
copy sha256_hashes.txt signatures\
Compress-Archive -Path signatures -DestinationPath signatures-v2024.11.24.zip
```

**Linux/Mac:**
```bash
mkdir signatures
cp md5_hashes.txt signatures/
cp sha256_hashes.txt signatures/
zip -r signatures-v2024.11.24.zip signatures/
```

### Шаг 3: Вычислите SHA256 хеш

**Windows PowerShell:**
```powershell
Get-FileHash signatures-v2024.11.24.zip -Algorithm SHA256
```

**Linux/Mac:**
```bash
sha256sum signatures-v2024.11.24.zip
```

**Пример вывода:**
```
SHA256: a3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
```

Скопируйте этот хеш и вставьте в `metadata.json` в поле `sha256`.

### Шаг 4: Загрузите на GitHub

1. Откройте https://github.com/T1desLuck/armaga-signatures
2. Создайте папку `databases/`
3. Нажмите "Add file" → "Upload files"
4. Загрузите:
   - `metadata.json`
   - `signatures-v2024.11.24.zip`
5. Commit changes

---

## 🔍 ИСТОЧНИКИ ДАННЫХ О ВИРУСАХ

### Откуда брать хеши вирусов:

#### 1. **VirusTotal** (Рекомендуется)
- **Сайт:** https://www.virustotal.com
- **API:** Есть (3 API ключа уже в `AppConfig.kt`)
- **Как использовать:**
  1. Загрузите подозрительный APK на VirusTotal
  2. Получите MD5 и SHA256 хеши
  3. Если файл определен как вредоносный → добавьте в базу

#### 2. **Koodous**
- **Сайт:** https://koodous.com
- **Описание:** База Android вредоносного ПО
- **Как использовать:**
  1. Зарегистрируйтесь
  2. Ищите вредоносные APK
  3. Скачивайте хеши и добавляйте в базу

#### 3. **MalwareBazaar**
- **Сайт:** https://bazaar.abuse.ch
- **Описание:** Открытая база вредоносного ПО
- **Как использовать:**
  1. Фильтруйте по Android
  2. Скачивайте CSV с хешами
  3. Конвертируйте в нужный формат

#### 4. **AndroTotal**
- **Сайт:** https://andrototal.org
- **Описание:** Анализ Android приложений
- **Как использовать:**
  1. Загрузите APK
  2. Получите отчет с хешами
  3. Добавьте в базу

#### 5. **Собственный анализ**
- Используйте эвристический движок приложения
- Анализируйте подозрительные приложения
- Добавляйте хеши в базу

### Пример скрипта для конвертации CSV в формат базы:

```python
import csv

with open('virustotal_export.csv', 'r') as csvfile:
    reader = csv.DictReader(csvfile)
    
    with open('md5_hashes.txt', 'w') as md5_file:
        with open('sha256_hashes.txt', 'w') as sha256_file:
            for row in reader:
                if row['md5']:
                    md5_file.write(f"{row['md5']},{row['name']},{row['severity']}\n")
                if row['sha256']:
                    sha256_file.write(f"{row['sha256']},{row['name']},{row['severity']}\n")
```

---

## 🔄 ОБНОВЛЕНИЕ БАЗЫ ДАННЫХ

### Когда обновлять:

- **Критические угрозы:** Немедленно
- **Новые вирусы:** 1-2 раза в неделю
- **Плановые обновления:** Раз в месяц

### Процесс обновления:

1. **Добавьте новые хеши** в `md5_hashes.txt` и `sha256_hashes.txt`

2. **Создайте новый ZIP:**
```powershell
Compress-Archive -Path signatures -DestinationPath signatures-v2024.11.25.zip -Force
```

3. **Вычислите новый SHA256:**
```powershell
Get-FileHash signatures-v2024.11.25.zip -Algorithm SHA256
```

4. **Обновите metadata.json:**
```json
{
  "version": "2024.11.25",
  "download_url": "https://raw.githubusercontent.com/T1desLuck/armaga-signatures/main/databases/signatures-v2024.11.25.zip",
  "sha256": "НОВЫЙ_SHA256_ХЕША",
  "size": 105600,
  "updated_at": "2024-11-25T14:30:00Z",
  "signatures_count": 5500
}
```

5. **Загрузите на GitHub:**
```bash
git add databases/
git commit -m "Update virus signatures to v2024.11.25"
git push origin main
```

6. **Проверьте доступность:**
- https://raw.githubusercontent.com/T1desLuck/armaga-signatures/main/databases/metadata.json

---

## 👑 АВТООБНОВЛЕНИЕ ДЛЯ PREMIUM

### Как работает:

1. **Только для Premium подписчиков**
2. **Автоматическая проверка** каждые 12 часов
3. **Загрузка только по Wi-Fi** (экономия трафика)
4. **Уведомление** после успешного обновления

### Настройка в приложении:

1. Пользователь покупает Premium подписку
2. В настройках включает "Автообновление баз"
3. WorkManager автоматически планирует задачу
4. Каждые 12 часов проверяется наличие обновлений

### Код реализации:

- `SignatureUpdateWorker.kt` - Worker для фоновой загрузки
- `SignatureUpdateScheduler.kt` - Планировщик задач
- `SettingsViewModel.kt` - Управление автообновлением

---

## ✅ ПРОВЕРКА РАБОТЫ

### 1. Проверка доступности файлов:

Откройте в браузере:
```
https://raw.githubusercontent.com/T1desLuck/armaga-signatures/main/databases/metadata.json
```

Вы должны увидеть JSON с метаданными.

### 2. Проверка в приложении:

1. **Главный экран:**
   - Нажмите "Обновить базы вирусов"
   - Должно появиться уведомление об обновлении

2. **Настройки:**
   - Внизу должна отображаться версия базы данных
   - Например: "2024.11.24"

3. **Сканирование:**
   - Запустите быстрое сканирование
   - База должна использоваться для проверки файлов

### 3. Проверка автообновления (Premium):

1. Купите Premium подписку
2. Включите "Автообновление баз" в настройках
3. Подождите 12 часов или измените интервал в `AppConfig.kt`
4. Проверьте уведомления

---

## 🔧 TROUBLESHOOTING

### Проблема: "Failed to download metadata"

**Решение:**
- Проверьте URL в `AppConfig.kt`
- Убедитесь, что файл `metadata.json` существует на GitHub
- Проверьте интернет-соединение

### Проблема: "SHA256 mismatch"

**Решение:**
- Пересчитайте SHA256 хеш ZIP файла
- Обновите значение в `metadata.json`
- Закоммитьте изменения на GitHub

### Проблема: "Database not updating"

**Решение:**
- Проверьте, что версия в `metadata.json` больше текущей
- Очистите кеш приложения
- Переустановите приложение

### Проблема: "Auto-update not working"

**Решение:**
- Убедитесь, что пользователь Premium
- Проверьте, что автообновление включено в настройках
- Проверьте логи WorkManager

---

## 📊 РЕКОМЕНДАЦИИ

### Размер базы:
- **Оптимально:** 100-500 KB
- **Максимум:** 5 MB
- При превышении - разбейте на части

### Версионирование:
- Используйте формат даты: `YYYY.MM.DD`
- Для нескольких обновлений в день: `YYYY.MM.DD-HH`

### Частота обновлений:
- **Критические:** Немедленно
- **Обычные:** 1-2 раза в неделю
- **Плановые:** Раз в месяц

---

## 🎉 ГОТОВО!

Теперь у вас есть полностью рабочая система обновления антивирусных баз данных!

**Что реализовано:**
✅ GitHub репозиторий настроен
✅ Автоматическая загрузка при первом запуске
✅ Ручное обновление через кнопку
✅ Автообновление для Premium (каждые 12 часов)
✅ Проверка целостности SHA256
✅ Использование в сканировании
✅ Работа без базы (fallback на эвристику)

**Успехов! 🚀**
